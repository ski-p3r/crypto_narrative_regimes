# Copy this file to .env and set a strong password

POSTGRES_DB=crypto
POSTGRES_USER=crypto_admin
POSTGRES_PASSWORD=CHANGEME_STRONG_PASSWORD

# Optional overrides for host/port when running tools locally
DB_HOST=127.0.0.1
DB_PORT=5432

# OpenAI API (required)
OPENAI_API_KEY=sk-CHANGE_ME

# Optional: HTTP(S) proxies to bypass exchange geoblocks
# Example: http://proxyhost:port or socks5://host:port
#HTTP_PROXY=
#HTTPS_PROXY=

# Optional: mock ingestion to bypass live exchange calls
#INGEST_MOCK=1

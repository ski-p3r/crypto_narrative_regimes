# Database Connection (PostgreSQL)
DB_URL=postgresql://user:password@localhost:5432/crypto

# OpenAI API Key (for narratives)
OPENAI_API_KEY=sk-yourkeyhere

# bk feature pipeline toggle (use bk wrappers instead of main)
USE_BK_FEATURES=0

# Optional: model for bk/gpt_feed.py
GPT_MODEL=gpt-4o-mini

# Paper trader toggle and settings
USE_PAPER_TRADER=0
PAPER_CONF_THRESHOLD=0.7
PAPER_LONG_REGIME=SPOT_IGNITION
PAPER_SHORT_REGIME=SPOT_COOLING
PAPER_TRADE_SIZE=1.0
